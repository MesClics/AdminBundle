{% extends "MesClicsAdminBundle::layout.html.twig" %} 

{% block deskHeadNavRails %}
    <a href="{{ path('mesclics_admin_home') }}" class="desk-head-nav_rails--item">Console d'administration</a>
    >
    <a href="{{ path('mesclics_admin_clients') }}" class="desk-head-nav_rails--item">Clients</a>
{% endblock deskHeadNavRails %}

{% block deskMain %} 
    <div class="oocss-widgets-collection-rows">
        {% include "MesClicsEspaceClientBundle:Widgets:client-search.html.twig" %}
        {% with {'isSlideshow': true } %}{% include "MesClicsEspaceClientBundle:Widgets:client-new.html.twig" %}{% endwith %}
    </div>

    {% if clients is defined and clients is not empty %}
        {% include "MesClicsEspaceClientBundle:Widgets:clients-list.html.twig" with clients %}
    {% endif %}
{% endblock deskMain%}
